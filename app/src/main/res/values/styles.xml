<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pink_normal</item>
        <item name="colorPrimaryDark">@color/pink_dark</item>
        <item name="colorAccent">@color/yellow_600</item>
    </style>

    <!-- 半透明背景設定 -->
    <drawable name="transparameter">#7f000000</drawable>

    <style name="BaseToolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:theme">@style/ToolbarTheme</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlActivated">@color/pink_normal</item>
    </style>

    <!-- CardView -->
    <style name="CardViewStyle" parent="CardView">
        <item name="android:layout_marginLeft">@dimen/space_8dp</item>
        <item name="android:layout_marginBottom">0dp</item>
        <item name="android:layout_marginRight">@dimen/space_8dp</item>
        <item name="android:layout_marginTop">@dimen/space_8dp</item>

        <item name="contentPadding">10dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <!-- Image Icon -->
    <style name="BaseImageIconStyle">
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="ImageIcon64dpStyle" parent="BaseImageIconStyle">
        <item name="android:layout_width">64dp</item>
        <item name="android:layout_height">64dp</item>
    </style>

    <style name="ImageIcon72dpStyle" parent="BaseImageIconStyle">
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
    </style>

    <style name="ImageIcon128dpStyle" parent="BaseImageIconStyle">
        <item name="android:layout_width">128dp</item>
        <item name="android:layout_height">128dp</item>
    </style>

    <!-- Image Toggle -->
    <style name="ImageToggleStyle">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:layout_marginRight">4dp</item>

        <item name="android:textOff">""</item>
        <item name="android:textOn">""</item>
    </style>

</resources>
